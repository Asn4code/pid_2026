# Cálculo de probabilidad en cadena de Markov
# P(X_n = b | X_{n-1} = a) = P(b|a) (probabilidad de transición)
# P(seq) = P(X_1) * P(X_2|X_1) * P(X_3|X_2) * ... * P(X_n|X_{n-1})
#
# Ejemplo: secuencia AC
# P(AC) = P(A) * P(C|A) = 0.25 * 0.20 = 0.05
#
# HMM: modelo oculto donde el estado no se observa directamente
# Estados ocultos: estado_0, estado_1, estado_2
# Emisiones observadas: A, C, G, T
# Viterbi: encuentra la secuencia de estados más probable
