# README de la entrega TC03

Completa este fichero desde la raíz de tu proyecto. Debe permitir repetir la
práctica sin ayuda adicional.

## Entorno

- Sistema (`uname -s`):
- Versión de Bash (primera línea de `bash --version`):
- Directorio desde el que se ejecutan los scripts:

## Datos

- Versión del corpus: 0.1.0
- Resultado de `sha256sum -c MANIFEST.sha256`:
- Número de listas de enteros y de ficheros FASTA observado:

## Programa 1: `buscar.sh`

- Uso y ejemplo:
- Semántica del recuento de comparaciones (lineal y binaria):
- Comparaciones observadas para `caso_busqueda.txt` con el objetivo 33:
- Interpretación de la diferencia entre O(n) y O(log n):

## Programa 2: `motivo.sh`

- Uso y ejemplo:
- Cómo cuentas las comparaciones de caracteres de la fuerza bruta:
- Ocurrencias y comparaciones para un fichero del corpus:
- Relación entre la longitud de la secuencia, la del motivo y el coste O(n·m):

## Programa 3: `escalado.sh`

- Uso y ejemplo:
- Columnas de `resultados/escalado.tsv` y su significado:
- Qué le ocurre a la columna cuadrática cuando `n` se duplica:

## Supuestos y límites

- Supuestos sobre el formato de las entradas:
- Un límite conocido de tus scripts:
- Diferencia entre medir operaciones y medir segundos:

## Autoría e IA

- Confirmo que los scripts, las pruebas y la evidencia son individuales: sí / no
- Uso de IA: ninguno / uso declarado
- Herramienta, consulta, fragmento afectado y forma de verificación, si procede:

[completar o escribir «no procede»]
