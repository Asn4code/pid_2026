# Evidencia individual TC03

## Identificación y entorno

- Código de estudiante:
- Fecha:
- Versión de la práctica: 0.1.0
- Vía: A (Linux local) / B (Google Colab)
- Salida de `uname -s`:
- Primera línea de `bash --version`:
- Directorio desde el que se ejecutaron los scripts:

## Datos

- Versión del corpus: 0.1.0
- Resultado de `sha256sum -c MANIFEST.sha256`:
- Número observado de listas de enteros / ficheros FASTA:

## Programa 1: búsqueda lineal frente a binaria

- Orden de ejecución:
- Comparaciones (lineal / binaria) para `caso_busqueda.txt` con el objetivo 33:
- Comparaciones para un objetivo ausente y para el último elemento:
- Justificación de por qué la binaria exige una lista ordenada:

## Programa 2: búsqueda de motivo por fuerza bruta

- Orden de ejecución:
- Ocurrencias, posiciones y comparaciones para un fichero del corpus:
- Caso de un motivo solapado (por ejemplo, `CG` sobre `adn_4.fasta`):
- Relación entre el coste O(n·m) y el tamaño de las entradas:

## Programa 3: crecimiento empírico

- Orden de ejecución:
- Tabla `resultados/escalado.tsv`:

```text
[pegar la tabla]
```

- Qué le ocurre a la columna cuadrática al duplicar `n` y por qué:

## Interpretación y límites

- Diferencia entre contar operaciones y cronometrar segundos:
- Un paradigma de diseño (fuerza bruta, voraz, divide y vencerás, dinámica) que reconozcas en tus scripts:
- Un límite o fallo conocido de tus scripts:

## Autoría e IA

- Confirmo que los scripts, las pruebas y la evidencia son individuales: sí / no
- Uso de IA: ninguno / uso declarado
- Herramienta, consulta, fragmento afectado y verificación, si procede:

[completar o escribir «no procede»]
