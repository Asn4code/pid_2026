# README de la entrega TC04

Completa este fichero desde la raíz de tu proyecto.

## Entorno

- Sistema (`uname -s`):
- Versión de Bash:
- Directorio desde el que se ejecutan los scripts:

## Datos

- Versión del corpus: 0.1.0
- Resultado de `sha256sum -c MANIFEST.sha256`:

## Programa 1: `ventanas.sh` (array)

- Uso y ejemplo:
- Por qué el acceso por índice a un array es O(1):
- Número de ventanas de longitud K sobre una secuencia de longitud L:

## Programa 2: `pila.sh` (pila / LIFO)

- Uso y ejemplo:
- Cómo la pila valida el emparejamiento de bases del ARN:
- Por qué un simple contador no basta y la pila sí:

## Programa 3: `cola.sh` (cola / LIFO frente a FIFO)

- Uso y ejemplo:
- Diferencia entre el orden de salida de una cola (FIFO) y una pila (LIFO):

## Costes y elección

- Tabla de costes de array, lista, pila y cola (acceso, inserción, borrado):
- Diferencia entre TDA e implementación:
- Diferencia entre almacenamiento contiguo y enlazado:

## Autoría e IA

- Confirmo que los scripts, las pruebas y la evidencia son individuales: sí / no
- Uso de IA: ninguno / uso declarado
- Herramienta, consulta, fragmento afectado y verificación, si procede:

[completar o escribir «no procede»]
